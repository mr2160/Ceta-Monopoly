import { TransakcijaTextPipe } from './transakcija-text.pipe';

describe('TransakcijaTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TransakcijaTextPipe();
    expect(pipe).toBeTruthy();
  });
});
